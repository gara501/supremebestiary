# Curated Apex Bestiary Agent Session

`Apex-Bestiary-Codex-Curated.jsonl` is a short, presentation-ready excerpt for the DEV.to Agent Sessions uploader.

It retains the narrative that matters for the Sanity Challenge:

1. map-provider evaluation and the OpenStreetMap decision;
2. the Bestiary field-archive visual direction;
3. the structured Sanity model and Path Two requirements;
4. the Vercel deployment decision;
5. the blocked base-map failure and its Leaflet + OpenStreetMap fix;
6. illustrated creature dossiers and optional procedural audio;
7. README and DEV.to submission preparation.

The source export included approval-review material and secret-pattern matches. This curated copy does **not** reproduce raw tool calls, environment values, command output, tokens, or keys. A validation step rejects the output if it detects common secret formats.

Before uploading, open the JSONL in a text editor and do one final manual review. Then upload it at <https://dev.to/agent_sessions/new>, set the session to public, and embed it in the `## Agent Session` section of `DEVTO_SUBMISSION.md`.
